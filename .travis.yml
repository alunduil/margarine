language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt --use-mirrors"
install: "pip install -r test_margarine/requirements.txt --use-mirrors"
# command to run tests
script: python -m test_margarine